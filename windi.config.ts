import { defineConfig } from "windicss/helpers";

export default defineConfig({
  shortcuts: {
    "flex-center": "flex items-center justify-center",
    "align-center": "flex items-center",
    "content-center": "flex justify-center"
  }
});
